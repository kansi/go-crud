version: '3.1'

services:

  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: go_crud
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
